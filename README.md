### ЧяВо
Завависимости: `python-telegram-bot`, `psutil`, `aiohttp`, `apscheduler`, `pytz`.
> [!TIP]
> бот сделан для удобного управления юзерботами ( управляет полностью только владелец бота)
> 
### установка??
Запусти файл, `Python 3.13.7` или ниже
```
python3 status-heroku.py
```
> [!TIP]
> запустите бота чтобы он создал `config.json`,
> откройте `config.json` и поставте свои значения

